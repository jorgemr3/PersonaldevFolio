export class Proyecto {
    constructor(id, titulo, descripcion, link, photo) {
      this.id = id;
      this.titulo = titulo;
      this.descripcion = descripcion;
      this.link = link;
      this.photo = photo
    }
  }