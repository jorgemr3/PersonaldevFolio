export class Proyecto {
    constructor(id, titulo, descripcion, archivo) {
      this.id = id;
      this.titulo = titulo;
      this.descripcion = descripcion;
      this.archivo = archivo;
    }
  }